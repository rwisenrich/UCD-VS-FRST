# UCD VS FRST Source Selector Tournament v3.0

V3 continues from the v2 selector/actuality reduction. It tests which additional principles can actually reduce selector, dynamics, actuality and finite-realization ambiguity.

Main conditional route:

`finite native alphabet + bounded actualization arity -> finite branching`

and

`strict descent of a finite/well-founded rank -> finite branch depth`.

Together with finite roots these imply a finite realized actualization tree with an explicit bound. This is not a UCD proof: UCD still has to derive bounded-arity primitive actualization and the descending rank from its own source law.

The selector tournament also proves that finite-group averaging canonically projects to the invariant subspace, while symmetry alone does not uniquely select a theory unless an additional singlet law and multiplicity-one condition hold.

Local full-package machine: 40/40 PASS; pytest 11/11 PASS.

Machine verdict: `V3_SOURCE_SELECTOR_TOURNAMENT_PASS__FINITE_ALPHABET_PLUS_RANK_DESCENT_ROUTE_FOUND__NATIVE_UCD_DERIVATION_REMAINS`.